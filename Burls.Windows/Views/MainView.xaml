<Window x:Class="Burls.Windows.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Burls.Windows"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Path=Browsers}" SelectedItem="{Binding Path=Browser}" DisplayMemberPath="Name" Margin="10,10,5,10" />

        <Grid Grid.Column="1" Margin="5,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,0,10">
                <Image Source="{Binding Path=Browser.IconImageSource}" Width="50" Height="50" />
                <Label Content="{Binding Path=Browser.Name}" />
                <Label Content="{Binding Path=Browser.ExecutablePath}" />
                <Label Content="{Binding Path=Browser.IconPath}" />
                <Label Content="{Binding Path=Browser.IconIndex}" />
                <Label Content="{Binding Path=Browser.Version}" />
            </StackPanel>

            <Button Content="Use" Grid.Row="1" />
        </Grid>
    </Grid>
</Window>
