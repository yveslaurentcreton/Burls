<Window x:Class="Burls.Windows.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Burls.Windows"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="Burls" Height="500" Width="850" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <DockPanel DockPanel.Dock="Top" Margin="10,10,10,0">
            <Label Content="URL:" />
            <TextBox Text="{Binding Path=RequestUrl}" VerticalContentAlignment="Center" />
        </DockPanel>
        
        <ItemsControl x:Name="BrowserProfiles" ItemsSource="{Binding Path=BrowserProfiles}" Grid.Row="1" Margin="10,5,10,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>

                    <UniformGrid x:Name="wpBrowserProfiles" Columns="4" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,10,10,10">
                        <UniformGrid.Resources>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Margin" Value="5" />
                            </Style>
                        </UniformGrid.Resources>
                    </UniformGrid>

                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="BrowserProfileButtonWidth" />
                        </Grid.ColumnDefinitions>
                        <Button Command="{Binding ElementName=BrowserProfiles, Path=DataContext.UseCommand}" CommandParameter="{Binding}" Width="190">
                            <Button.Content>
                                <StackPanel Orientation="Vertical">
                                    <Image Source="{Binding Path=Browser.IconImageSource}" Width="{Binding Path=Browser.IconImageSource.Width}" Height="{Binding Path=Browser.IconImageSource.Height}" />
                                    <Label Content="{Binding Path=Name}" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>

                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
